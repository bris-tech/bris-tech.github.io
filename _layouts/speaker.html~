---
layout: default
---
<h2>{{ page.title }}</h2>
<div id="post">
{{ content }}
</div>

{% assign gotcomingup = false %}
{% for post in site.categories['event'] %}
<p>{{ post.speaker }}
<p>{{ page.speakerid }}
{% if post.categories contains 'archive' %}
{% elsif post.speaker == page.speakerid %}
{% assign gotcomingup = true %}
{% endif %}
{% endfor %}
{% if gotcomingup %}
<div>
  <h3>Coming Up...</h3>
  <ul>
  {% for post in site.categories['event'] %}
  {% if post.categories contains 'archive' %}
  {% elsif post.speaker == page.speakerid %}
    <li><a href="{{ post.url }}">{{ post.date | date_to_string }} - {{ post.title }}</a></li>
  {% endif %}
  {% endfor %}
  </ul>
</div>
{% endif %}

{% assign gotprevious = false %}
{% for post in site.categories['archive'] %}
{% if post.speaker == page.speakerid %}
{% assign gotprevious = true %}
{% endif %}
{% endfor %}
{% if gotprevious %}
<div>
  <h3>Previous Talks</h3>
  <ul>
  {% for post in site.categories['archive'] %}
  {% if post.speaker == page.speakerid %}
    <li><a href="{{ post.url }}">{{ post.date | date_to_string }} - {{ post.title }}</a></li>
  {% endif %}
  {% endfor %}
  </ul>
</div>
{% endif %}
