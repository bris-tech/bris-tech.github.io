---
layout: default
title: #bristech - Calendar
---

<div id="home">
  <h1>Coming Up...</h1>
  {% for post in site.categories['event'] reversed %}
  {% if post.categories contains 'archive' %}
  {% else %}
  <h3><a href="{{ post.url }}">{{ post.date | date_to_string }} - {{ post.title }}</a></h3>
  <p>Tagged with: {{ post.tags | join: ', ' }}</p>
  {% endif %}
  {% endfor %}
</div>
